# ДЕПО — сайт выставки

## Запуск

Header и footer подгружаются через `fetch`. **Двойной клик по HTML не подойдёт** — нужен локальный сервер:

```bash
npx serve .
```

Открыть: http://localhost:3000/pages/contacts.html
